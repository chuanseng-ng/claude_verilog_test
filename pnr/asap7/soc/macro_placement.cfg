# =============================================================================
# SoC ASAP7 Hard-Macro Placement Configuration
# =============================================================================
#
# Hierarchy strategy (bottom-up hierarchical):
#   - CPU hard macro (rv32i_cpu_top):  130×130 µm hard macro view
#   - GPU hard macro (gpu_top):        340×340 µm hard macro view
#   - SRAM:  synthesised black-box; placed by LibreLane PDN macro grid
#   - Flat:  axi4_crossbar, axi_lite_interconnect, dma_engine, uart_controller,
#            spi_controller, timer, interrupt_controller, sram_controller,
#            boot_rom, pll stubs  (~2 K gates total)
#
# Die:  520×520 µm   (0 0 520 520)
# Core: 500×500 µm   (10 10 510 510)
#
# Layout strategy:
#   GPU (340×340): lower-left, pinned against SW corner with 5 µm die-edge +
#                  5 µm halo = 15 µm from die edge.
#   CPU (130×130): lower-right, separated from GPU by >=10 µm halo gap.
#                  GPU right edge: 15+340=355.  CPU origin X: 365 (10 µm gap).
#                  CPU right edge: 365+130=495, within core boundary 510.
#   Upper half (Y>355): flat logic, AXI crossbar, peripheral routing channels.
#
# Halo: 5 µm on all sides enforced by LibreLane PDN macro grid halo.
#
# Coordinate origin: (0,0) = die lower-left.
# Instance paths must match the soc_top instantiation names.
# =============================================================================

# GPU hard macro -- lower-left.
# Origin: (15, 15) -> leaves 5 um die-edge clearance + 5 um halo.
# GPU footprint: 340x340 um.  Right edge @ 15+340=355.  Top edge @ 15+340=355.
# Orientation: N (no flip) -- AXI master ports on east face toward crossbar.
u_gpu 15 15 N

# CPU hard macro -- lower-right.
# Origin: (365, 15) -> right edge @ 365+130=495 (15 um from core boundary 510).
# GPU right edge (355) + 10 um gap = 365.  CPU footprint: 130x130 um.
# Top edge @ 15+130=145.
# Orientation: N -- AXI master ports on north/east face toward crossbar.
u_cpu 365 15 N

# =============================================================================
# SRAM black-box macros (sram_controller's internal SRAMs)
# Placed by LibreLane's macro PDN grid; no explicit placement coordinates needed
# for the synthesised SRAM stub.  If LibreLane requires explicit placement for
# the 256x32 stub instance inside u_sram, add it here once the instance path
# is known from synthesis output.
# =============================================================================
