# Development and testing dependencies
pytest>=8.0.0
pytest-cov>=4.0.0
mypy>=1.8.0
pylint>=3.0.0
ruff>=0.1.0

# Verification infrastructure
cocotb>=1.8.0,<2.0  # cocotb 2.x removed the units= kwarg used throughout tb/
pyuvm>=3.0.0,<3.1   # pinned alongside cocotb 1.x

# Pre-commit hooks
pre-commit>=3.5.0
