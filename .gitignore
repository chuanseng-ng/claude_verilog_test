

#Ignore vscode AI rules
.github/instructions/codacy.instructions.md
.codacy/

# Claude AI
tmpclaude*
.claude/

# Verilator
obj_dir/
work_uvm/
logs/
*.vcd

# cocotb
# Bead claude_verilog_test-f97: BOTH forms are needed.  The trailing slash matches
# only real directories, so with SIM_BUILD_ROOT pointed at scratch the in-tree
# symlinks were left untracked-but-committable on any clone that lacks a local
# .git/info/exclude entry.  The slashless form covers the symlinks.
sim_build*/
sim_build*
__pycache__/
*.pyc
*.pyo
*.xml

# Python testing and QA
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

bash.exe.stackdump

# Python
venv/
.venv/

# Test results and artifacts
results/
reports/
coverage*.dat
coverage_tb_*.dat

# PD-flow runs
pnr/librelane/runs
pnr/openlane/runs
pnr/openlane1/runs
pnr/freepdk45/runs
pnr/asap7/cpu/runs
pnr/asap7/gpu/runs
pnr/asap7/soc/runs
pnr/sky130/cpu/runs
pnr/sky130/soc/runs

# PDK paths
pnr/librelane/rv32i_cpu_top

# Verilog
*.swp
slpp_all/
pnr/slpp_all

# Added by code-review-graph
.code-review-graph/

# Graphify
graphify-out/GRAPH_REPORT.md

# Agents
memory/

# HDL property graph (hdl-kgraph) - large local Kuzu DB
.hdl-kgraph/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
pnr/asap7/soc/soc_top_sv2v.v

# HLS (GH #119): Bambu build artefacts. Generated Verilog lives in /nobackup
# (see hls/README.md) and never lands in the tree, but a run invoked by hand from
# a source dir drops HLS_output/ beside the sources.
HLS_output/
hls/**/HLS_output/
